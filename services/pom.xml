<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.13</version>
        <relativePath/>
    </parent>

    <groupId>com.repairshop.saas</groupId>
    <artifactId>repair-shop-services</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Repair Shop SaaS - Microservices</name>
    <description>Spring Boot microservices for Mobile Repair Shop SaaS</description>

    <properties>
        <java.version>25</java.version>
        <jjwt.version>0.12.5</jjwt.version>
        <aws.sdk.version>2.32.11</aws.sdk.version>
        <springdoc.version>2.8.17</springdoc.version>
    </properties>

    <modules>
        <!-- Libraries, not services. Must build before the modules that depend on them. -->
        <module>common-media</module>
        <module>common-subscription</module>
        <module>auth-service</module>
        <module>user-service</module>
        <module>shop-service</module>
        <module>ticket-service</module>
        <module>technician-service</module>
        <module>inventory-service</module>
        <module>marketplace-service</module>
        <module>pickup-service</module>
        <module>notification-service</module>
        <module>subscription-service</module>
        <module>master-data-service</module>
        <module>order-service</module>
    </modules>

    <!--
        Inherited by every service module. Actuator gives all 12 services a uniform
        /actuator/health, which the EC2 deploy workflow uses as its post-deploy
        health gate. Only the health endpoint is web-exposed (Spring Boot default)
        and details are hidden, so this adds no public surface beyond UP/DOWN.
    -->
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <!--
                AWS SDK v2 BOM. Media (device images, KYC documents, shop artwork)
                lives in S3 behind the media.ggfix.in CloudFront distribution; the
                BOM keeps the s3/auth/regions artifacts on one version.
            -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
